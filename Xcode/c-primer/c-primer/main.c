#include "main.h"

int main(int argc, char **argv) {
  helloWorld(argc, argv);
  variable(argc, argv);
  globalVariables(argc, argv);
  function(argc, argv);
  ifThen(argc, argv);
  pointer(argc, argv);
  array(argc, argv);
  structTest(argc, argv);
  unionTest(argc, argv);

}
